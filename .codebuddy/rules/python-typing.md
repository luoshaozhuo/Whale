---
alwaysApply: false
paths: "{src,tests}/**/*.py"
---

# Python 类型规则

- 使用 Python 3.10+ 类型标注语法。
- 公共函数和公共方法必须补全参数类型与返回值类型。
- 核心内部函数如有明确输入输出，也应补全类型标注。
- 类型正确性以仓库中的 mypy 配置和检查结果为准。
- 不要无理由引入 `Any`、`type: ignore`、`noqa`。
- 如必须忽略类型检查，应将范围缩到最小，并说明原因。
- 不要为了通过类型检查而重写与当前任务无关的业务逻辑。